---
    interface Props {
        title: string;
    }

    const { title} = Astro.props
---

<section class="w-full border-t pt-10 flex flex-col gap-4">
    <h2 class="uppercase">{title}</h2>
    <article class="grid grid-cols-12 gap-y-5 md:gap-y-10 w-full ">
        <slot>
    </article>
</section>